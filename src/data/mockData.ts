// Upcoming recurring expenses aren't editable yet — this stays a simple
// static reminder list until that becomes its own feature. Empty by
// default; there's nothing to pre-fill honestly since it's not tied to
// your real fixed costs yet.
export const mockUpcoming: { id: string; label: string; amount: number; date: string }[] = [];
